import { DashboardApp } from "@/components/dashboard/dashboard-app";

export const metadata = {
  title: "Dashboard | Finance",
  description: "Track balances, spending, and transactions.",
};

export default function DashboardPage() {
  return <DashboardApp />;
}
